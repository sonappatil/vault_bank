vulnerabilities: []
misconfigurations:
  - id: AWS-0011
    paths:
      - "infra/frontend-s3-cloudfront/main.tf"
    expired_at: 2026-09-30
    statement: >-
      AWS Free Tier POC uses NGINX ModSecurity with OWASP CRS as the
      compensating web application firewall. Production CloudFront
      must use AWS WAF before this exception expires.
  - id: AWS-0132
    paths:
      - "infra/frontend-s3-cloudfront/main.tf"
    expired_at: 2026-09-30
    statement: >-
      AWS Free Tier POC uses S3-managed AES-256 encryption. Production
      must use SSE-KMS with a customer-managed key before this
      exception expires.
  # phase27-production-trivy-exceptions
  - id: KSV-0014
    paths:
      - "gitops/overlays/production/frontend-production-image-patch.yaml"
    expired_at: 2026-10-31
    statement: "Approved exception: this file is a Kustomize image-only patch; the effective frontend Deployment inherits its hardened securityContext from the base manifest. Review by expiry."

  - id: KSV-0118
    paths:
      - "gitops/overlays/production/frontend-production-image-patch.yaml"
    expired_at: 2026-10-31
    statement: "Approved exception: this file is a Kustomize image-only patch rather than a standalone Deployment; pod and container securityContext are supplied by the hardened base manifest. Review by expiry."

  - id: KSV-0108
    paths:
      - "gitops/overlays/production/shared-runtime-aliases.yaml"
    expired_at: 2026-10-31
    statement: "Approved exception: ExternalName Services are intentional internal Kubernetes DNS aliases for the shared staging PostgreSQL, Redis and RabbitMQ runtime; access remains restricted by NetworkPolicy. Review by expiry."
secrets: []
licenses: []
